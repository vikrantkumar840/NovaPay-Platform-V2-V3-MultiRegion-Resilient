apiVersion: velero.io/v1
kind: BackupStorageLocation

metadata:
  name: default
  namespace: velero

spec:
  provider: aws

  objectStorage:
    bucket: novapay-velero-backup-primary

  config:
    region: ap-south-1
